kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: service-b
spec:
  subdomain: service-b
  to:
    kind: Service
    name: service-b
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None

